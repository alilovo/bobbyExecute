# Production Readiness - required env vars
# Copy to .env and fill in values

# Safety (M0)
LIVE_TRADING=false

# Replay mode (M1) - deterministic traceIds when true
REPLAY_MODE=false

# RPC (M4)
RPC_MODE=stub
RPC_URL=https://api.mainnet-beta.solana.com
